// 数据库连接信息
const config = {
  // 启动端口
  port: 30,

  // 数据库配置
  database: {
    DATABASE: "blogsql",
    USERNAME: "root",
    PASSWORD: "root",
    PORT: "3306",
    HOST: "localhost"
  }
};

module.exports = config;
